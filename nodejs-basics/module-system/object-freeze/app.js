const utils = require('./utils')

const priceList = [12, 40404, 32, 123, 4, 666]

utils.even = 2
utils.odd = 1

console.log(utils)
console.log(utils.even(priceList))

utils.users.age = 18
console.log(utils.users)
